export enum KeyboardTypes {
  DEFAULT = 'default',
  NUMBER_PAD = 'number-pad',
  DECIMAL_PAD = 'decimal-pad',
  NUMERIC = 'numeric',
  EMAIL = 'email-address',
  PHONE = 'phone-pad',
  URL = 'url',
}